// Módulo createApp
import { createApp } from "vue";

// Importa o arquivo App.vue
import App from "./App.vue";

// Monta o App.vue no elemento com id #app (Renderiza o App.vue no elemento com id #app)
createApp(App).mount("#app");
